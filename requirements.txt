# requirements.txt — AI Study Buddy Capstone Project
# Install with: pip install -r requirements.txt
langgraph
langchain-groq
chromadb
sentence-transformers
streamlit
python-dotenv
# Core LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0

# Vector DB + Embeddings
chromadb>=0.4.0
sentence-transformers>=3.0.0

# Streamlit UI
streamlit>=1.40.0

# Environment variables
python-dotenv>=1.0.0

# RAGAS Evaluation (optional but recommended)
ragas>=0.1.0
datasets>=2.0.0

# Standard library modules used (no install needed):
# ast, re, math, datetime, uuid, os
